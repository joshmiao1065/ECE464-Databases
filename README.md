# ECE464-Databases

> **Agents:** Read `LESSONS.md` at the start of every conversation. It contains
> debugging patterns accumulated across development sessions — async SQLAlchemy
> pitfalls, TF eager execution conflicts, subprocess isolation, and more.
> Update it immediately when you root-cause a new bug.
